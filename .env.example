# Copia esta variable en Vercel > Settings > Environment Variables.
# Nunca publiques la conexión real en GitHub ni en el frontend.
DATABASE_URL=postgresql://USER:PASSWORD@HOST/DATABASE?sslmode=require
